
```
while(node){
    while(node->left){
        //1 找最左节点
        stk.push(node)
        node= node->left;
    }
    if(node->right){
        //2 最左节点的右节点不空，继续找最底层最左
        stk.push(node);
        node = node->right;
    }else{
        //3 空，抵达最底层最左
        exe(node)
        lnode = node;
        node = stk.top
        //4 返回根
        while(lnode==node->right){
            exe(node)
            stk.pop
            lnode = node;
            node = stk.top;
        }
        if(node)
        node = node->right;
    }
}
```

